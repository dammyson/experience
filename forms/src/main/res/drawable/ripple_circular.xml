<ripple xmlns:android="http://schemas.android.com/apk/res/android"
    android:color="@color/white_pale_form">
    <item android:id="@android:id/mask">
        <shape
            android:shape="oval"
            android:tint="@color/white_pale_form">

            <solid android:color="@color/white_pale_form" />
        </shape>
    </item>
</ripple>
